/**
 * Development environment configuration.
 *
 * Defines the environment-specific variables used when the Angular
 * application is running in development mode.
 *
 * @constant environment
 *
 * @property production Indicates whether the application is running in production mode.
 * @property apiUrl Base URL of the AutoService backend API used during development.
 */
export const environment = {
  production: false,
  apiUrl: 'https://autoservice-api.ddns.net'
};
